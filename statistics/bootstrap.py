from typing import List, Dict, Any

def generate_bootstrap_samples(
scores: List[float],
n_iterations: int = 1000
) -> List[List[float]]:
"""Generate bootstrap samples."""
pass

def bootstrap_metric_distribution(
scores: List[float],
n_iterations: int = 1000
) -> List[float]:
"""Generate bootstrap metric distribution."""
pass

def compute_bootstrap_mean(
bootstrap_scores: List[float]
) -> float:
"""Compute bootstrap mean."""
pass
